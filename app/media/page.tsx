import { notFound } from "next/navigation"

export default function MediaPage() {
  // Temporarily redirect to not-found
  notFound()

  // The original code is preserved in page.tsx.bak
  return null
}
